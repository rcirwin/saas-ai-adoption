# Research Run — 2026-06-10 — DRY_RUN (0 prospects researched)

## Result: DRY_RUN — no researchable prospects in queue

- **target**: all-identified
- **limit**: 10
- **force**: false

## Counts

- Researched: 0
- Not-a-fit: 0
- Cache-hits: 0
- Errors: 0
- **DRY_RUN**: queue contains only sourcer-flagged DELETED/duplicate rows

## Queue state

- `status=identified` rows total: **39**
- Non-DELETED / researchable rows: **0**

All 39 identified rows are sourcer-flagged for deletion. Every row has either:
- an `id` prefixed `DELETED-DUP-DO-NOT-USE...` / `DELETED-DUP...` / `DELETED...`, or
- a `company` value of `DELETED-DUPLICATE` / `DELETED`,
and their `notes` explicitly say "ignore this row", "Duplicate ... ignore", "Skip", "remove", "not independent", or "not bootstrapped; remove".

The researcher correctly excluded all `DELETED`-prefixed rows per the runbook (never research rows the sourcer has flagged for removal).

## Fit score distribution

None (no prospects researched).

## Top 5 prospects by fit score

None.

## Flagged issues / queue-pollution alert (REPEAT from 2026-06-09)

**This is the second consecutive run blocked by the same queue pollution.** The 2026-06-09 run already flagged 39 sourcer-flagged DELETED/dup rows consuming the `identified` queue. They have NOT been hard-deleted; the count is unchanged at 39.

Notable repeat-offender duplicate IDs (collisions — multiple distinct rows share one id):
- `DELETED-DUP-DO-NOT-USE-referralcandy-2` appears ~7x (marker.io, stripo.email, contentstudio.io, vbout.com, engagebay.com, coresignal.com, helpcrunch.com)
- `DELETED-DUP-DO-NOT-USE-linkinize` appears 2x (identical Linkinize rows)
- `DELETED-DUP-DO-NOT-USE` (bare) appears for wpfusion.com

**Action required from sourcer:** hard-delete all `DELETED`-prefixed rows and resolve the id collisions. Until then, every researcher run on `all-identified` returns DRY_RUN and the Outreach Writer (3h downstream) has no new scored rows. Several of the flagged rows wrap *real* companies that may be worth re-sourcing as clean rows (e.g. canny.io / Sarah Hum, survicate.com, paperform.co, referralcandy, rivo.io) — but only the sourcer should re-create those as non-DELETED `identified` rows; the researcher will not resurrect flagged rows.

## Sheet writes

None attempted (no prospects to write). No MANUAL_ENTRIES_NEEDED.

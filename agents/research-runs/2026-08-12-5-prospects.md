# Research Run — 2026-08-12 — 5 prospects

Target: `all-identified` (default limit 10). Slice: the final 5 of the 15-row 2026-08-11 directory batch — exactly the five named as remaining by the 2026-08-11 run. **Queue now empty.**

## Counts

| Metric | Count |
|---|---|
| Researched (fit 3+) | 2 |
| Not-a-fit (fit 1–2) | 3 |
| Cache hits | 0 |
| Sheet write errors | 0 |
| Prospects skipped / not found | 0 |

**Average fit score: 2.60** (lowest of any scored run to date; previous low 3.00).

## Fit score distribution

| Score | Count | Prospects |
|---|---|---|
| 5 | 0 | — |
| 4 | 1 | omti |
| 3 | 1 | fluency-inc |
| 2 | 3 | jetsoft, stone-profit-systems, kudurru-stone |
| 1 | 0 | — |

## Ranked prospects (all 5 — batch smaller than the top-5 slot)

| # | id | Fit | ai_posture | recommended_angle |
|---|---|---|---|---|
| 1 | omti | 4 | none | Workflow-first / Timing — AI transcription is automating the labour RB9 schedules; open on the 2018 RB9 ground-up rebuild, prove the blueprint on MetaRecords or RB9 Lite |
| 2 | fluency-inc | 3 | none | Workflow-first opened on the competitive AI gap (Boostlingo / LanguageLine LLAI / Interpreters Unlimited); moat is the validated medical-interpreting test data, not a chatbot. Pre-empt the Size objection |
| 3 | jetsoft | 2 | strategic | None — not-a-fit |
| 4 | stone-profit-systems | 2 | none | None — not-a-fit (keep as category proof point for smaller stone-fab vendors) |
| 5 | kudurru-stone | 2 | none | None — not-a-fit; 12-month revisit |

## Why this run scored low

Three of five died on **scale**, and each on a different edge of the ICP band. This batch is a clean illustration that the ARR gate, not the workflow gate, is what actually decides these:

- **stone-profit-systems — above the ceiling.** ~$9M revenue against 35 employees (~$257K/head). Unusually, this figure *passes* the headcount sanity check, so it was trusted rather than discarded. Attractive on every non-scale axis.
- **kudurru-stone — below the floor.** ~$40/user/month category norm, deliberately lower for light roles; $500K needs ~1,000+ seats. Nearest rival Cyanic Job Book was killed on the identical test on 08-11.
- **jetsoft — figure internally impossible.** $5.2M against 4–15 employees, seed-stage classification and Made Smarter SME accelerator participation. Discarded per the frontrunner-professional rule. Carried a second independent blocker (AI already on the core surface).

## Flagged issues

### Inconclusive / gated research

- **fluency-inc (fit 3) — ARR is the gating question and could not be resolved in either direction.** 5 employees and "50+" customer organisations. No price point is published anywhere, so the arithmetic that decided kudurru-stone and jetsoft could not be run. If hospital language-access contracts are five-figure the company clears the floor comfortably; if they are per-seat SaaS it does not. Scored 3 deliberately — thin evidence in *both* directions is what a 3 means. **Outreach writer: do not send until ARR is confirmed and a contact exists.**
- **omti (fit 4) — ARR estimated, not confirmed.** Aggregators report ~$7M/16 employees; treated as inflated per the growjo rule, with realistic ARR inferred from "100s of subscription customers on a 40-year franchise" as $1–5M. This inference is the sole basis for clearing the ICP band. Held at 4 rather than 5 partly for this reason.

### Sheet data corrections needed (sourcer)

- **stone-profit-systems `contact_name` is wrong.** Sheet says "Dirk Burrowes, Founder" — Dirk Burrowes founded **Vytek** (Fitchburg, MA, 1989). Available sources name **Ravi Rudraraju** as CEO of Stone Profit Systems. Fifth instance of the "verify sourcer's contact_name as hypothesis, not truth" pattern.
- **stone-profit-systems `arr_estimate`** is `unknown`; should be `5M+`.
- **jetsoft `arr_estimate` ($5,200,000) and `employee_count` (15) are both suspect.** RocketReach reports 4 employees as of Apr 2026 against the same revenue figure. Row should be flagged, not silently scored around.
- **omti and fluency-inc have no contact data at all** (no name, role, LinkedIn or email). Both are the fit 3+ rows from this run, so both are currently **unreachable**. This is the recurring G2/Capterra/directory-source pattern — directory-sourced rows arrive without contacts. Sourcer must backfill before the outreach writer can act on either.

### Environment

- **Egress 403 — 9th consecutive run.** `WebFetch` is blocked for every domain including a control fetch of `example.com`, which returned `EGRESS_BLOCKED` from the proxy. `curl "$HTTPS_PROXY/__agentproxy/status"` shows the proxy healthy and enabled with `recentRelayFailures: []`, confirming this is a **policy block, not a transport fault** — no local workaround exists and TLS was not touched. Entire run conducted on WebSearch snippets. The 08-11 escalation deadline has now passed unactioned; this is measurably costing fit-score precision (fluency-inc's pricing page would very likely have resolved a fit 2-vs-4 question in one fetch).
- `pip install cffi` required again on a fresh container before any `sheet.py` call.
- **`research_cache` schema conflict — 9th consecutive run.** Tab is columns A–L with no `fit_score` and no `ai_posture`, but the agent definition's step 8 instructs writing both. Omitted both again; all 5 upserts clean. Note the knock-on: because `research_cache` stores neither field, the **step 5 cache-hit path cannot work as specified** — it says to restore `fit_score` and `ai_posture` from the cached row, but those values are not in the cache. A future cache hit will have to re-derive them from `fit_assessment`. Worth reconciling the spec against the sheet.

### Queue / tombstone status

Full-tab verify: **1,601 rows = 1,512 real + 89 tombstones (78 distinct ids)**. Real statuses: researched 812 / not-a-fit 675 / disqualified 24 / declined-engagement 1 / **identified 0**. No blank-status rows hiding outside the filter.

- **The tombstone backlog changed character this week — it grew from 40 to 89.** Diffing against 08-07 (1,586 rows = 1,546 real + 40 tombstones; researched 804 / not-a-fit 717): `researched` reconciles exactly (804 + 6 on 08-11 + 2 today = 812), but `not-a-fit` *fell* from 717 to a reconciled-expected 724 → actual 675. **~49 not-a-fit rows were converted into tombstones.** Someone has begun purging by tombstoning rather than deleting, which does not reduce the read cost — it moves rows from one filtered-out bucket into another while worsening the distinct-id collision hazard (78 distinct ids across 89 rows). Worth raising with the caller: the 11-week-old cleanup request now appears to have been actioned in a way that makes the problem bigger, not smaller.
- `identified` is now **0**. On the established cadence the next sourcer batch is due **Tue 2026-08-18**. An empty queue before then is a cadence gap, **not starvation** — do not escalate. That rule has now held three times.

## MANUAL_ENTRIES_NEEDED

None. All 5 `research_cache` upserts and all 5 `prospects` updates succeeded.

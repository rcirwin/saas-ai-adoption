# Prospect Research Run — 2026-08-05

**Target:** `all-identified` · **Limit:** 10 · **Force:** false · **Staleness window:** 90 days

## Counts

| Metric | Count |
|---|---|
| Researched (fit 3+) | 3 |
| Not-a-fit (fit 1–2) | 2 |
| Cache hits | 0 |
| Errors (unrecovered) | 0 |
| **Total processed** | **5** |

Queue read returned **44 rows: 39 `DELETED-*` tombstones filtered client-side, 5 real.** All 5 carried `created_at=2026-08-04`, so the created_at-ascending sort was a no-op and they were taken in sheet order. These are exactly the 5 the 2026-08-04 run predicted would remain (bestnotes, flex-rental-solutions, buildingreports, sbi-software, iris-works).

**The `identified` queue is now empty.** Next run returns `DRY_RUN` unless the sourcer adds rows. Per the established cadence rule the sourcer ships on Tuesdays — the next batch is due **Tue 2026-08-11**. An empty queue before then is a cadence gap, not starvation; do not escalate.

## Fit Score Distribution

| Score | Count | Prospects |
|---|---|---|
| 5 | 0 | — |
| 4 | 2 | buildingreports, flex-rental-solutions |
| 3 | 1 | sbi-software |
| 2 | 2 | bestnotes, iris-works |
| 1 | 0 | — |

**Average fit score: 3.00**

## Top Prospects by Fit Score

| # | Prospect | Score | AI posture | Recommended angle |
|---|---|---|---|---|
| 1 | **buildingreports** | 4 | none | Data readiness / competitive moat — 120M-device inspection history as an un-mined predictive asset; cite Inspect Point and ZenTrades marketing AI inspection and AI scoring at the same buyers |
| 2 | **flex-rental-solutions** | 4 | none | Competitive moat / agent-ready — lead with Stagera's named conquest page and AI ops assistant, then pivot workflow-first |
| 3 | **sbi-software** | 3 | bolt-on | Workflow-first / what-to-build — probe whether the "AI insights" module acts or only reports; time to the Cultivate'26 launch |
| 4 | bestnotes | 2 | strategic | None — do not pursue |
| 5 | iris-works | 2 | none | None — do not pursue |

Only 3 prospects scored 3+, so the top-5 table above is the full batch.

## Notable Findings

**flex-rental-solutions — sharpest competitive hook of the batch.** An AI-native entrant, **Stagera (stagera.ai), runs a dedicated conquest page at `stagera.ai/compare/flex/`** attacking Flex by name on on-premise/hybrid deployment and per-module add-on pricing, while leading with an "AI-powered operations assistant." This is the bidwrangler "third parties colonising your AI surface" pattern in its strongest observed form — not an adjacent integration but a named, verifiable competitive attack. Deep workflow (quote → contract → pull/pack → logistics → invoice over RFID-tagged inventory), in-band 3M-5M/30 emp, named long-tenured founder-CEO Chris Stein.

**buildingreports — cleanest durable AI gap.** Fire/life-safety compliance is the regulated-vertical `none`-posture goldmine: NFPA/AHJ workflows are ones generic horizontal AI never reaches, so missing AI is durable rather than negligent. Two reinforcing signals: rivals **Inspect Point and ZenTrades market "AI-powered inspections" and "AI scoring" and name BuildingReports in comparison content as the documentation-only option lacking end-to-end workflow** — a third-party-verified gap rather than our inference — and **120M+ inspected devices across 1.85M inspections is a genuine proprietary data asset** for predictive device-failure and deficiency triage, mapping directly onto deliverable #3.

**sbi-software — good workflow, unverifiable economics.** Horticulture ERP with a genuinely constraint-heavy domain (production planning of living perishable inventory across grow cycles, availability forecasting, big-box order allocation, seed-line automation). Capped at 3 by two independent limits: ARR is genuinely unknown (no revenue figure in any source; **employee counts conflict irreconcilably — Tracxn 190 vs BBB 27**; the widely-quoted $1.2B is customer GMV, not SBI revenue), and they already market a named "AI insights" module in the Cultivate'26 replatform.

**bestnotes — three independent kills.** Owler reports **$5.4M revenue, above the $5M ceiling** (sheet already flagged 5M+); AI posture is **strategic on the core clinical surface** (Videra Sidekick Notes with a published 47,500 clinician-hours-saved milestone, Perspectives AI predictive insights and orchestration, natural-language EHR search, automated intake routing) which is the cybake/crosstrax "no discovery gap" shape; and the sheet carries **no contact name, role, LinkedIn or email**.

**iris-works — ARR-by-price-point resolved a 6x aggregator conflict.** Sources split between $5.9M and $500K-$1M. At a $9.95-$25/month price point, $5.9M implies ~20,000 active subscribers — implausible for a niche photographer CRM behind HoneyBook/Tave/Dubsado and irreconcilable with 4-8 employees. Credible figure is $500K-$1M, so the engagement-as-%-of-revenue test kills it: **$30K against ~$750K is ~4% of annual revenue**. Founder Meredith Gradle is reachable and hook-rich (2019 Tory Burch Fellow, podcast appearances) but reachability does not rescue out-of-band economics.

## Flagged Issues

### Contact gaps blocking outreach
- **bestnotes** — no contact_name, contact_role, contact_linkedin or contact_email. Moot at fit 2, but note the pattern: `directory`-sourced rows keep arriving without contacts.
- All three fit-3+ rows (**buildingreports**, **flex-rental-solutions**, **sbi-software**) have a named contact and role but **blank `contact_email` and blank `contact_linkedin`**. The sourcer must backfill before the Outreach Writer can act. Named contacts: Brett Brewster (Founder & CEO), Chris Stein (Founder & CEO), Aaron Allison (Co-Founder & CEO). Aaron Allison's public LinkedIn surfaced in research as `linkedin.com/in/aaallison/` — candidate for backfill, sourcer to verify.

### Egress blocked — 5th consecutive snippet-only run
Per the control-fetch-first tactic, a **Wikipedia control fetch was run as the first network call and returned 403**, confirming WebFetch is globally dead rather than prospect-domain-specific. `curl "$HTTPS_PROXY/__agentproxy/status"` returned `enabled:true` with an **empty `recentRelayFailures`** array → this is an egress **allow-list policy** decision, not a transport fault; per `/root/.ccr/README.md` it was not retried or routed around.

**Quantified cost this run:** no prospect's own website could be read, and the vendor site is where AI gets announced — so **all three `none`/`bolt-on` postures are provisional**, and **both fit-4s were held at 4 rather than 5 purely on unverifiable facts** (independent ARR confirmation and founder LinkedIn posting cadence for buildingreports and flex-rental-solutions). Restoring egress would plausibly convert one or both to 5s. `sources_checked` uses the `website-403(egress-blocked)` convention. **Escalating again — this is the 5th consecutive run and now the sole reason no prospect has scored 5 in five runs.**

### Schema inconsistency — 8th consecutive run
Agent-definition step 8 instructs writing `fit_score` and `ai_posture` into `research_cache`, and step 5 instructs restoring both from a cache hit. The live tab and `agents/data/prospects-sheet-schema.md` both define `research_cache` as **columns A–L only** (`prospect_id` → `recommended_angle`), with no such columns. Per the "reference wins" rule both were omitted from cache upserts and written only to the `prospects` tab (cols L, M); all 5 upserts succeeded cleanly.

**Consequence, restated: a cache hit can never restore `fit_score` or `ai_posture`.** This has now been reported eight runs running. The agent definition needs amending, or the two columns adding to the tab.

### Tombstone backlog — unchanged since 2026-06-09 (~8 weeks)
39 `DELETED-*` rows still returned by the `status=identified` filter and dropped client-side. Previously measured as 40 tombstones across only 32 distinct ids (`-referralcandy-2` x7 covering 7 different real companies, `-knowify-2026-05-28` x2, `-linkinize` x2). This remains a **primary-key correctness hazard**, not merely queue noise, since all agents dedupe on column A. Researcher does not resurrect flagged rows.

## MANUAL_ENTRIES_NEEDED

None. All 10 sheet writes (5 `research_cache` upserts + 5 `prospects` updates) succeeded on first attempt.

## Environment Notes

- Fresh container: `pip install --user cffi` was required again before `sheet.py` would run (installed cffi 2.1.1 + pycparser 3.0). Without it every invocation throws `ModuleNotFoundError: No module named '_cffi_backend'` → `pyo3_runtime.PanicException`. Predicted by memory and fixed in pre-flight before the first sheet call.
- `research_staleness_days = 90` (from `config`). ICP band confirmed 500000–5000000.
- Writes issued through a Python subprocess arg-list driver (no shell) to avoid bash `$`-expansion on ARR dollar values, with 3s pacing. No 429s.
- **`fit_notes` length was asserted before any write this run** — the pre-flight caught 2 rows over the 200-char cap (flex-rental-solutions 205, sbi-software 201) and aborted before writing, so no corrective second pass was needed. Final lengths 182–199. This is the fix for the repeated over-cap failures on 07-29 and 08-04.
- 3 reads, 10 writes, 0 unrecovered errors.

# Prospect Research Run — 2026-08-04 (10 prospects)

Target: `all-identified` · limit 10 (default) · force=false · staleness=90d

## Counts

| Metric | Count |
|---|---|
| Researched | 10 |
| → `researched` (fit 3+) | 8 |
| → `not-a-fit` (fit 1–2) | 2 |
| Cache hits (skipped) | 0 |
| Sheet write errors | 0 |
| Avg fit score | **3.30** |

Queue at start: **15 real `identified`** (54 rows returned, 39 `DELETED-*` tombstones filtered client-side). Took the first 10 by `created_at` ascending — all 15 share `created_at = 2026-08-04`, so slice order follows sheet order.

## Fit score distribution

| Score | Count | IDs |
|---|---|---|
| 5 | 0 | — |
| 4 | 5 | cemsites, qt9, bidwrangler, ecanvasser, jersey-watch |
| 3 | 3 | purplepass, webcemeteries, kicksite |
| 2 | 2 | crosstrax, auctionmethod |
| 1 | 0 | — |

No fit-5. Four of the five 4s were held down by a single unverifiable factor (founder LinkedIn cadence or ARR ceiling) — see Egress note.

## Top 5 by fit score

| # | ID | Fit | Posture | Recommended angle |
|---|---|---|---|---|
| 1 | `qt9` | 4 | none | AI-for-users + data-readiness — open with his own Quality Magazine quote on AI/ML in calibration, extend to CAPA and audit evidence |
| 2 | `jersey-watch` | 4 | none | Workflow-first constraint-satisfaction — "your scheduler is the product"; reference his own alternatives posts |
| 3 | `ecanvasser` | 4 | none | Data-readiness + agent-ready — 400M leads and an open API; anchor on the field-sales repositioning |
| 4 | `bidwrangler` | 4 | bolt-on | Agent-readiness + bolt-on reframe — Gavelist/Estimint are already inside your platform; technical ex-Amazon CTO buyer |
| 5 | `cemsites` | 4 | none | Workflow-first — 13 years of structured burial/plot/deed data as the moat |

## Not-a-fit detail

- **`crosstrax`** (2) — AI gap already closed. Ships ML transcription, auto report generation from field notes, cross-case pattern detection, document review and drafting: a workflow-first posture, not a bolt-on. Compounded by economics — ~$800K revenue on 3 staff with $35/$65/$105-per-month plans makes a $30K engagement ~4% of annual revenue. ARR clears the floor but the stage is wrong.
- **`auctionmethod`** (2) — 2–10 employees, no verifiable revenue, and a SaaS + custom-development services hybrid that signals sub-floor product ARR and a fragmented roadmap. Instructive in-batch contrast: `bidwrangler` is the same category at 20 staff and confirmed 1M–3M and scored 4. Revisit in 12 months; founder's "vibe coding with AI" post is a strong hook if they scale.

## Flagged issues

### 1. `webcemeteries` — contact is stale, blocks outreach (action: sourcer)
Sheet lists **Eric A. Robuck, Founder** as contact. Current leadership is **Nick Timpe (President & CEO)** with Jason Cavett (COO); Eric Robuck now appears as Founder/CEO/President at **The Valander Group**. The named buyer has moved on. Product shape was otherwise 4-grade (high workflow complexity, `none` posture, public GitHub org, fresh 40-report launch) — scored **3** under the founder-replaced/CEO-transition precedent. Re-verify the buyer and this becomes a 4. Contact columns left untouched per role boundary.

### 2. `kicksite` — no named contact at all (action: sourcer)
`contact_name` empty and the CEO surfaces publicly only as "Jason N."; family-owned, no exec page. There is no one to write to. Also ARR-unverifiable: sheet says unknown, aggregators claim an implausible **$25–50M** for a 40-person dojo-software vendor, while published $49–$199/month tiers against 2,000+ schools implies roughly **$2–3M**. Capped at 3 per the anonymous-vendor + unverifiable-ARR rule.

### 3. `qt9` — ARR may exceed the ICP ceiling
Sheet says 3M–5M, but the founder's own podcast appearance (The Scale Up Show) cites **"zero to $6 million"**. If ~$6M is current they are over the $5M ceiling. Held at 4 rather than 5 on that risk alone — everything else (25+ regulated quality operations, `none` posture against AI-pushing peers, exceptionally reachable founder) is 5-grade. Worth pursuing; confirm revenue on the call.

### 4. Egress blocked — WebSearch-only run, 4th consecutive (action: operator)
`WebFetch` returned **403 for every host**, including a Wikipedia control fetch run as the first network call. `curl "$HTTPS_PROXY/__agentproxy/status"` shows `enabled:true` with an **empty `recentRelayFailures`** → this is an egress **allow-list policy** decision, not a transport fault; per `/root/.ccr/README.md` it was not retried or routed around. No prospect website could be read directly; all 10 postures rest on search snippets and third-party directory/review content.

**Data-quality consequence:** the vendor's own site is where AI gets announced, so every `none` posture in this run is **provisional**. This is the 4th consecutive snippet-only run and the degradation is now concrete and measurable — it cost fit-score precision on four separate prospects (cemsites, ecanvasser, jersey-watch held at 4 purely because founder LinkedIn cadence was unverifiable; qt9 on unconfirmable revenue). **Restoring egress would likely convert two or three of this batch's 4s into 5s.** Escalated again.

## Environment notes

- `scripts/sheet.py` was dead on this fresh image with `ModuleNotFoundError: No module named '_cffi_backend'` → `pyo3_runtime.PanicException`. Fixed with `pip install --user cffi` (cffi 2.1.1 + pycparser 3.0). Now a reliable fresh-image step — predicted by memory and fixed pre-flight.
- **Schema inconsistency, 7th consecutive run.** Agent definition steps 5 and 8 instruct writing/restoring `fit_score` and `ai_posture` via `research_cache`, but `agents/data/prospects-sheet-schema.md` defines that tab as columns **A–L ending at `recommended_angle`** — neither column exists. Reference wins, so both were omitted from the cache upserts (all 10 succeeded cleanly). Standing consequence: **a cache hit can never restore `fit_score` or `ai_posture`** — only narrative fields live in cache. The definition needs amending or the two columns adding.
- Tombstone backlog **unchanged and unpurged since 2026-06-09 (~8 weeks)**: 39 `DELETED-*` rows still returned by a `status=identified` read. All agents dedupe on column A, so this remains a primary-key correctness hazard, not just queue noise.

## Queue state

**5 real `identified` rows remain** — next slice, in order:

| ID | ARR | Contact | Note |
|---|---|---|---|
| `bestnotes` | **5M+** | none | Above ICP ceiling and no contact — likely not-a-fit on ARR alone |
| `flex-rental-solutions` | 3M-5M | Chris Stein | Event production rental ERP |
| `buildingreports` | 1M-3M | Brett Brewster | Fire/life-safety compliance — compliance vertical, promising shape |
| `sbi-software` | unknown | Aaron Allison | Horticulture grower ERP |
| `iris-works` | 1M-3M | Meredith Gradle | Photography studio management |

Sourcer cadence held: batch landed **Tue 2026-08-04** as predicted on 07-31, ending the cadence gap. No starvation alert.

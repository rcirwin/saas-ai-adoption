# Template Performance Snapshot

Derived snapshot. Source of truth is the `outreach_log` Sheet tab. Regenerated by `frs-outreach-writer` on every run.

**Generated:** 2026-08-11
**Policy mode:** `EXPLORATION` (even deterministic split across active email variants)

---

## Scoring basis

A **scored send** = an `outreach_log` row where `status = sent` AND `response_status` is non-blank. Drafts, skips, bounces and unsent rows never count toward ranking.

| Metric | Value |
|---|---|
| Total `outreach_log` rows | 2,387 |
| Rows with `status = sent` | 532 |
| **Scored sends (sent + dispositioned)** | **0** |

Every `response_status` in the log is still blank, so `reply_rate` and `call_rate` are undefined for every (template, category, ai_posture) cell. No ranking table can be produced.

---

## Active email variants (the A/B set)

| Variant | Role | Scored sends | Sent (any disposition) | Drafted total | Gate (`MIN_SCORED_PER_VARIANT = 20`) |
|---|---|---|---|---|---|
| `hyper-personalized-email` | control | 0 | 187 | 344 | below threshold |
| `email-short-question` | challenger | 0 | 0 | 129 | below threshold |
| `email-proof-led` | challenger | 0 | 0 | 134 | below threshold |

All three variants sit below the 20-scored-send gate, so the policy stays in **exploration**: each prospect is assigned deterministically and evenly by
`idx = sum(ord(c) for c in prospect_id) % 3`.

### This run's assignment (11 emails)

| Variant | Count | Prospects |
|---|---|---|
| `email-short-question` | 5 | usewhale, vevox, vidalytics, wiztopic, yunbit |
| `email-proof-led` | 3 | useberry, valora-digital, worknice |
| `hyper-personalized-email` | 3 | vexels, virtualspeech, wiserreview |

8 of 11 landed on a challenger arm, which is the send-starved side.

---

## LinkedIn channel selection

No scored sends exist for any LinkedIn template either, so selection falls back to the default. Connects use `linkedin-connect-observation-first` (mirror observation first, fixed closing line, no FRS naming) per the 2026-06-23 human correction.

---

## Blockers

**STANDING BLOCKER (unchanged).** 532 sent rows, 0 dispositioned. Until `response_status` is filled in for even a sample of sent rows, no template can be ranked and the A/B test produces nothing. This is the single highest-leverage manual action available.

**SECOND-ORDER BLOCKER (unchanged).** Per-variant *sent* counts are control 187, `email-short-question` 0, `email-proof-led` 0. The two challengers have been drafted 263 times combined and have never been sent once. Even a complete `response_status` backfill would score only the control and yield no A/B read. Getting challenger-arm drafts actually sent is a prerequisite ahead of disposition backfill.

# Template Performance Snapshot

Derived snapshot. Source of truth is the `outreach_log` Sheet tab. Regenerated by `frs-outreach-writer` on every run.

**Last regenerated**: 2026-08-12
**Log rows read**: 2401
**Sent rows**: 532
**Scored sends** (`status = sent` AND `response_status` non-blank): **0**

## Policy Mode: EXPLORATION

`MIN_SCORED_PER_VARIANT = 20`. Every active email variant is below the threshold, so variant assignment is deterministic and even:

```
idx = sum(ord(c) for c in prospect_id) % 3
variant = ["hyper-personalized-email", "email-short-question", "email-proof-led"][idx]
```

LinkedIn channels continue to fall back to `linkedin-connect-default` (no scored data to rank on).

## Active Email Variants

| Variant | Role | Drafted (all time) | Sent | Scored | reply_rate | call_rate |
|---|---|---|---|---|---|---|
| `hyper-personalized-email` | control | 333 | 187 | 0 | n/a | n/a |
| `email-short-question` | challenger, brevity | 129 | 0 | 0 | n/a | n/a |
| `email-proof-led` | challenger, credibility-first | 133 | 0 | 0 | n/a | n/a |

## Ranking Table

Empty. Ranking requires scored sends grouped by (`template_used`, `category`, `ai_posture`). There are zero scored sends across every cell, so no cell can be ranked and no exploitation decision is available.

## Two Standing Blockers

These are distinct problems and fixing one does not fix the other.

**1. Disposition backfill.** All 532 sent rows have a blank `response_status`. Until Ryan fills in `accepted` / `replied` / `no-response` / `declined` on the sent cohort, reply_rate and call_rate are undefined for every template. This has been the standing blocker since 2026-07-28.

**2. Challenger arms have never been sent.** `email-short-question` and `email-proof-led` have 262 drafted rows between them and **zero** sent. Even a complete backfill of the 532 sent rows would score only the control and produce no structural comparison. The A/B experiment cannot conclude until short-question and proof-led drafts actually go out.

**The ask to Ryan is both:** disposition the sent cohort, AND send some challenger-variant drafts so the split has data on both arms. Today's run added 4 proof-led and 2 short-question drafts, so there is challenger inventory sitting ready for review.

## Non-Email Templates Observed in `outreach_log`

`existing-draft` (309 sent), `linkedin-connect-default` (14 sent), `handwritten` (9), `on-the-fly-draft` (5), `pre-draft` (3), plus small cleaned/stripped variants. None are scored either. `existing-draft` is a send-time label rather than a real template ID, so it will never be rankable against the A/B set even after backfill.

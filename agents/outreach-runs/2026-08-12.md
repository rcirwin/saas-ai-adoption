# Outreach Run. 2026-08-12

Target: `all-researched`. Cap 15 (`config.outreach_daily_cap`), cadence 7 days (`config.follow_up_cadence_days`).

**6 drafts / 6 prospects.** Under cap because the eligible pool was genuinely exhausted at 6, not because of a limit. The 2026-08-11 run consumed the previous parallel-email pool, and those 11 prospects are now inside the 7-day cadence window.

## Counts by Channel

| Channel | Drafts |
|---|---|
| email | 6 |
| linkedin-connect | 0 |
| linkedin-dm | 0 |
| referral | 0 |

Every one of the 6 already has LinkedIn touched (4 with a genuinely SENT connect, 1 pending, 1 skipped-wrong-profile). Email is the only untouched channel on all of them, so this is an email-only run by construction rather than by preference.

## Template Distribution

Email A/B is in **EXPLORATION** mode. Zero scored sends exist across all three variants, so assignment is the deterministic even split `sum(ord(c) for c in prospect_id) % 3`.

| Variant | Count | Prospects |
|---|---|---|
| `email-proof-led` | 4 | streak, stablebuzz, thundertix, tability |
| `email-short-question` | 2 | tutorbird, tokens-studio |
| `hyper-personalized-email` | 0 | none landed on this index |

All 6 drafts sit on a challenger arm. That is the send-starved side of the experiment and is worth prioritising at review time.

## Drafts

| Prospect | Contact | Fit | Posture | Variant | Hook | Path |
|---|---|---|---|---|---|---|
| streak | Aleem Mawani, Co-Founder & CEO | 3 | strategic | proof-led | His own framing that he runs a YC company like a bootstrapped product, set against the NL Q&A and magic columns already shipped | `agents/outreach-drafts/2026-08-12-streak-email.md` |
| stablebuzz | Colin Schmidt, Founder/CEO | 3 | none | proof-led | The Discussion Topic of the Week community video series, which is user research he already does in public | `agents/outreach-drafts/2026-08-12-stablebuzz-email.md` |
| tutorbird | Dan Santoni, President | 3 | none | short-question | Port 443 runs My Music Staff, TutorBird and AthletaDesk on one admin spine | `agents/outreach-drafts/2026-08-12-tutorbird-email.md` |
| thundertix | Dawn Green, Founder | 3 | bolt-on | proof-led | Her published high-renewal cohort at 3.1x LTV, a number produced but never acted on | `agents/outreach-drafts/2026-08-12-thundertix-email.md` |
| tokens-studio | Mike Kamminga, CEO & Co-founder | 3 | none | short-question | Moving off the Figma plugin into a token platform under Hyma, plus the Headless Design System concept he co-created | `agents/outreach-drafts/2026-08-12-tokens-studio-email.md` |
| tability | Sten Pittet, Co-Founder & CEO | 3 | strategic | proof-led | AI Mode already shipped, against his Atlassian vantage on what becomes a system of record | `agents/outreach-drafts/2026-08-12-tability-email.md` |

All 6 are fit-3. Second consecutive all-fit-3 run.

### Mirror discipline against prior sent touches

4 of the 6 have a connect note that was genuinely SENT in early July. For each, the prior `message_text` was pulled and the email deliberately mirrors a **different** founder_perspective dimension:

- **streak** connect mirrored industry_ai_view (Workspace AI squeezing Gmail-native, agents orchestrating or routing around). Email mirrors product_strategy plus concerns: the bootstrapped-operating constraint means one AI bet, and the only uncopyable one is fourteen years of inbox record.
- **thundertix** connect mirrored ai_posture (AI event descriptions, then patron renewals as an agent surface). Email mirrors product_strategy: the non-ticket half she chose to build, and the measurement-to-action gap on her own 3.1x cohort number.
- **tokens-studio** connect mirrored concerns (Figma absorbing Variables, defending the plugin edge). Email mirrors industry_ai_view: the Headless Design System bet extended to the case where the consumer of the token layer is a model, not a designer.
- **tability** connect mirrored ai_posture (AI Mode as a step past text-gen, OKRs as state agents act on). Email mirrors product_strategy: his Atlassian arc, and whether Tability owns goal state or reads it.

`tutorbird` (connect `already-pending`) and `stablebuzz` (connect `skipped-wrong-profile`, never sent) are first-reaching by email.

### Objection preempt

2 strategic-posture prospects, `streak` and `tability`. `objections.md` loaded. Both drafts credit the shipped AI explicitly in the opening beat, then pivot past the feature, which preempts "we already have AI" before it gets raised.

## Verification Flags for Ryan

- **stablebuzz VERIFY_CONTACT.** The 2026-07-06 LinkedIn attempt was dispositioned `skipped-wrong-profile`. `/in/colin-schmidt` shows a Software Platforms founder at Zynim Inc with no equestrian signal, so it is likely a different Colin Schmidt. The email `colin@stablebuzz.com` is a company-domain address rather than one derived from the wrong name, so it stands on its own, but confirm the human before sending.
- **Inferred addresses.** `streak` (`[inferred] aleem@streak.com`) and `tokens-studio` (`[inferred] mike@tokens.studio`) are pattern-guessed. Tokens Studio now operates under Hyma, so `mike@tokens.studio` carries some bounce risk. A web check confirmed Mike Kamminga is still CEO and co-founder, so the person is right even if the domain needs a look.
- **tokens-studio research correction.** The 2026-07-06 log note "now at Hyma" reads as a departure but is not one. Hyma is the parent company behind Tokens Studio and Kamminga is its CEO. The prospect is live.
- **tutorbird research correction.** `research_cache.competitive_landscape` lists MyMusicStaff as a competitor. It is a sibling product. Port 443 Inc of Burlington Ontario runs My Music Staff, TutorBird and AthletaDesk, with Dan Santoni as President. The cache row should be corrected.

## Skipped and Why

Full queue was 812 researched prospects. Classification order: departed pre-filter first, then cadence, then contact presence, then a per-id `outreach_log` join with substring channel matching.

| Bucket | Count | Meaning |
|---|---|---|
| `no_untouched_sendable` | 613 | Both LinkedIn and email already touched with a non-void, non-skipped row. Nothing left to open with. |
| `no_contact_at_all` | 92 | Neither `contact_linkedin` nor `contact_email` present after sentinel normalization (`unknown` / `n/a` / `none` / `tbd` treated as blank). Sourcer dependency. |
| `departed` | 57 | Founder-departed disqualifier fired. See below. |
| `in_cadence` | 44 | `last_outreach_date` within 7 days. Includes the 14 prospects drafted on 2026-08-11. |
| `parallel_email` | 6 | **Drafted this run.** |

### Departed pre-filter detail

Ran as two independent filters per the 2026-08-11 codification, both required:

- **(a)** word-boundary regex over `contact_name` + `contact_role` + `fit_notes` + `notes` + `research_summary` + the full `research_cache` blob. Term hits: `acquired by` 16, `new ceo` 10, `departed` 5, `sold to` 5, `moved on` 3, `exited` 2, `former ceo` 2, `no longer ceo` 1.
- **(b)** `status == skipped-founder-departed` on any prior `outreach_log` row for that `prospect_id`.

57 total disqualified. **18 of those would otherwise have been sendable this run**, which is three times the size of the batch that actually shipped:

- Caught by (b) only, invisible to the regex: `genoo`, `teamwave`, `text-blaze`, `postaga`, `brandbook`, `ohmyfi`, `onesta-farm`, `levo-mx`, `doorbell`, `mindqube`, `sanebox`, `contractor-compliance`, `accountsportal` (fit 4), `servebolt`, `spoks`.
- Caught by (a) only, invisible to the log filter: `order-printer-pro` (`contact_role` reads "Founder (sold to Shop Circle 2024)"), `employeeconnect` (`research_cache` says "Founder Craig Macdonald no longer CEO"), `snap-surveys`.

The two filters remain non-redundant and each caught cases the other missed, exactly as recorded on 2026-08-11. All 18 flagged for re-research with corrected contact names.

`snap-surveys` is worth a human look specifically. The regex fires on "founder-departed long ago but James Brewer current operator" in `fit_notes`, which is arguably a false positive since a current operator is named. It was skipped anyway because the sheet's `contact_name` is Steve Jenkins while `fit_notes` names James Brewer, and the email is `[inferred]` off the Steve Jenkins name. That name conflict is an independent reason to skip, so the outcome is right even if the regex reasoning was not.

## MANUAL_ENTRIES_NEEDED

None. All 6 `outreach_log` appends and all 6 `prospects` updates succeeded on the first attempt.

## Standing Blockers

1. **Zero scored sends, still.** 2401 log rows, 532 sent, `response_status` blank on every single one. Template performance is undefined and EXPLORATION mode persists indefinitely until Ryan dispositions the sent cohort.
2. **Challenger arms have never been sent.** `email-short-question` 129 drafted / 0 sent. `email-proof-led` 133 drafted / 0 sent. Backfilling the 532 sent rows would score only the control. The A/B needs actual challenger sends, and this run added 6 more to the ready pile.
3. **92 prospects with no contact info at all.** Long-running sourcer dependency.

## Next Follow-up Window

2026-08-19 for all 6 drafted this run.

The pool for the next run is thin. Almost everything sendable is now either inside cadence or fully touched on both channels. The 18 departed-but-otherwise-sendable prospects are the single largest recoverable pool, and re-research with corrected contacts would reopen them.

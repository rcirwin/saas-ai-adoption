# Outreach Run — 2026-05-14

LinkedIn outreach drafts after the 2026-05-14 full-CRM re-research (`agents/research-runs/2026-05-14-56-prospects.md`).

Scope per request: fit-5 + fit-4 only; skip fit ≤ 3. Channel: LinkedIn — `linkedin-connect` for first-touch, `linkedin-dm` as the post-connection follow-up for prospects with a prior `linkedin-connect` row in `outreach_log`.

## Counts

- Drafts written: **15** (daily cap)
- By channel: `linkedin-dm` = 12, `linkedin-connect` = 3
- By fit score: fit-5 = 4 (3 DMs + 1 connect), fit-4 = 11 (9 DMs + 2 connects)
- `outreach_log` appends: 15 / 15
- `prospects` updates: 15 / 15 (last_outreach_date=2026-05-14, follow_up_due=2026-05-21)
- Errors: 0

## Template distribution

| Template | Count |
|---|---|
| `linkedin-dm-default` | 12 |
| `linkedin-connect-default` | 3 |

Template performance analysis: `outreach_log` has 33 rows total now (18 prior + 15 today), all `status: drafted` with blank `response_status`. No reply data → fell back to defaults. Eighteenth consecutive run with no template bias possible.

## Drafts (15)

### Fit-5

| Prospect | Channel | Hook |
|---|---|---|
| `userlist` | linkedin-dm | $1M ARR + 5-person team focused-positioning; counter "AI workflow gen" |
| `outseta` | linkedin-dm | all-in-one stack as agent-readiness substrate (unified data) |
| `savio` | linkedin-dm | Housecall Pro / Tettra / Reclaim customer list — picked Savio for signal clarity vs Productboard's AI noise |
| `financial-cents` | linkedin-connect | Inc 5000 2025 + Month End Close ship + Karbon contrast |

### Fit-4 (DMs after prior connect)

| Prospect | Channel | Hook |
|---|---|---|
| `plausible` | linkedin-dm | AI-tool traffic dashboard = canary for agent-era discovery; 18K subs / 260B pageviews |
| `simple-analytics` | linkedin-dm | AI chat is table-stakes; agents don't need cookies (privacy-first angle for agent-era) |
| `savvycal` | linkedin-dm | Overlay UX bet vs agent-to-agent scheduling future |
| `noloco` | linkedin-dm | Nola + AI columns + n8n is closer to agent-orchestration than prompt-to-app |
| `tenantcloud` | linkedin-dm | Apartments.com / Realtor.com integrations + release cadence as operator-AI foundation |
| `everhour` | linkedin-dm | AI Tracker beta + Asana embed; auto-categorize vs agent-reads-time-data |
| `splynx` | linkedin-dm | WISPA 2025 + MetroFibre / Liquid Telecom + real API; which ISP-ops to make agent-callable |
| `betterproposals` | linkedin-dm | 10K brands (United Rentals/Aspen Dental); separate proposal-AI (gimmick) vs operator-AI (signal) |
| `wp-umbrella` | linkedin-dm | 8.5M backups + rules engine as substrate for "explain update broke layout" / "predict risk" |

### Fit-4 (connect, no LinkedIn URL — flagged in draft frontmatter)

| Prospect | Channel | Hook | Blocker |
|---|---|---|---|
| `paperbell` | linkedin-connect | Laura Roeder MeetEdgar pedigree + "no bots, no AI" stance + 14K coaches | LinkedIn URL not in `prospects.contact_linkedin` |
| `jobtread` | linkedin-connect | Deloitte Tech Fast 500 #6 + EY Entrepreneur 2025 + 7,500 builders + AI Connector bolt-on | LinkedIn URL not in `prospects.contact_linkedin` |

Note: per user brief, `prospects.notes` (column T) backfilled the founder names for these. Decision-maker names are in the drafts; Ryan must search/find the LinkedIn profile URL before sending. Same blocker applies to `financial-cents` (fit-5 connect).

## Skipped

| Prospect | Fit | Reason |
|---|---|---|
| `zenmaid` | 5 | In cadence — connect drafted 2026-05-12 (`follow_up_due=2026-05-19`) |
| `emailoctopus` | 5 | In cadence — connect drafted 2026-05-12 (`follow_up_due=2026-05-19`) |
| `castos` | 4 | Over daily cap (15); no LinkedIn URL — defer to next run |
| `refiner` | 4 | Over daily cap; no LinkedIn URL — defer to next run |
| `docupilot` | 4 | Over daily cap; no LinkedIn URL — defer to next run |
| `dubsado` | 4 | Over daily cap; no LinkedIn URL — defer to next run |
| `guideflow` | 3 | Out of scope (user: skip fit ≤ 2 with focus on 5 & 4) |
| `kickserv` | 3 | Out of scope |
| `usermaven` | 3 | Out of scope |
| `ewebinar` | 3 | Out of scope |
| `survicate` | 3 | Out of scope |
| `carepatron` | 3 | Out of scope |
| `tettra` | 3 | Out of scope |
| `referralcandy` | 3 | Out of scope |

## Notes / Open Items

1. **DM-after-connect interpretation**: 12 prospects had prior `linkedin-connect` drafts in `outreach_log` going back to 2026-04-21. With `response_status` still blank across the log, the prior runs treated these as disposition-blocked. The user brief explicitly asked for "connect notes + first DM" on the freshly enriched research, so this run treats them as ready-for-DM (assumes Ryan sent the connects and at least some were accepted). If any connects were NOT sent or NOT accepted, the corresponding DMs in this run should be held back until status is known.

2. **Contact-discovery dependency**: 7 fit-4/5 prospects (`docupilot`, `dubsado`, `paperbell`, `jobtread`, `refiner`, `financial-cents`, `castos`) have decision-maker names backfilled into `prospects.notes` (column T) by the researcher but no LinkedIn URL in column J. Drafts for `financial-cents`, `paperbell`, `jobtread` were written using the backfilled names; Ryan needs to look up the LinkedIn profile URL before sending. Sourcer should backfill column J.

3. **Hook diversity**: each new DM uses a distinct hook from the prior connect for the same prospect. Connect hooks cited public-arc / category-pressure framings (UI Breakfast, contrarian-bootstrap, Drip-arc, anti-AI piece). DM hooks pivot to product-specific substrate (data architecture, customer mix, feature signal, integration topology).

4. **Voice**: all drafts pass banned-word check (no `leverage`, `synergy`, etc.). Connect notes verified ≤ 300 chars via Python `len()`. DMs all ≤ 100 words.

## Next follow-up window

- 2026-05-14 batch: `follow_up_due = 2026-05-21`
- 2026-05-12 batch (zenmaid, emailoctopus, carepatron): `follow_up_due = 2026-05-19`

## Action for human (Ryan)

- Review drafts in `agents/outreach-drafts/2026-05-14-*.md`
- Source LinkedIn URLs for `financial-cents`, `paperbell`, `jobtread` (CONTACT_BLOCKER flag in frontmatter)
- Disposition prior `outreach_log` rows (set `response_status` = accepted / no-response / pending) so future runs can target accurately
- Mark sent rows `status = sent` after sending

---
prospect_id: tokens-studio
channel: email
template_used: email-short-question
angle: if tokens are infrastructure rather than a Figma feature, the open question is whether a model writes against the token contract or reads the CSS and skips it
date_drafted: 2026-08-12
status: drafted
contact: Mike Kamminga (CEO & Co-founder), [inferred] mike@tokens.studio
hook_source: Tokens Studio moving off the plugin into a token management platform under Hyma, plus the Headless Design System concept he co-created
subject: quick one on tokens when the generator isn't human
prior_touch: 2026-07-06 linkedin-connect SENT. That note mirrored concerns (Figma absorbing Variables, defending the plugin edge with AI token migration and naming). This email mirrors product_strategy and industry_ai_view instead.
founder_perspective:
  product_strategy: Moving from the Figma plugin to a design token management and design system automation platform under Hyma. 264K plugin users behind it.
  ai_posture: None observed in product. Tokens are programmatic, not modelled.
  concerns: Figma absorbing the core with Variables and code-connect, which puts a plugin-resident business on the platform line.
  industry_ai_view: Co-created the Headless Design System, which reads as a bet that the token layer is infrastructure rather than a design tool feature.
mirror_choice: industry_ai_view: the Headless Design System bet that tokens are infrastructure, extended to the case where the consumer of that infrastructure is a model
---
Subject: quick one on tokens when the generator isn't human

Mike,

Tokens Studio moving off the plugin and into a token platform under Hyma reads like a bet that the token layer is infrastructure, not a Figma feature. Headless Design System is the same bet said out loud.

So the question. When the thing assembling an interface is a model rather than a designer, does the token set become the contract that model writes against, or does the model read the compiled CSS and skip the layer entirely?

Would value comparing notes on how you're framing it.

Ryan
---

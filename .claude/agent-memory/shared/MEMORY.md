# Shared Memory (cross-agent)

All FRS agents read this file at the start of every run and append when a learning applies to the whole system.

Rules:
- Patterns only, not individual data points.
- Keep entries to 1–2 lines each.
- If a learning only affects one agent, put it in that agent's private MEMORY.md instead.
- Prune stale entries quarterly.

---

## Cross-cutting learnings

(empty — populated as the system runs)

## Categories to deprioritize

(empty — populated when the researcher and outreach writer both see low conversion in a category)

## Hooks and angles that reliably work

(empty — populated when the content writer and outreach writer both see high response in a pattern)
